[package]
name = "minimal-clock-core"
version = "0.2.1"
edition = "2026"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
windows = { version = "0.52", features = [
    "Win32_Foundation",
    "Win32_System_SystemInformation",
    "Win32_Networking_WinHttp",
] }